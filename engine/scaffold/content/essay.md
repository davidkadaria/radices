# Project Title

Write your content here in Markdown. This file is rendered to `index.html`
by the shared build engine.

## A section

Regular paragraphs, **bold**, _italic_, [links](https://example.com), lists,
footnotes[^1] and GitHub-flavored Markdown are all supported.

[^1]: Footnotes render under the label configured in `engine/parseMarkdown.js`.
